{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "python --version && pip install -r requirements-frontend.txt --break-system-packages && python sync_config.py --env production",
  "outputDirectory": "."
}